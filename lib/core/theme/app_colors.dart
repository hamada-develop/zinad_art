import 'package:flutter/material.dart';

class AppColors {
  AppColors._();

  // Primary palette — dark premium with gold accent
  static const Color primaryDark = Color(0xFF1A1A2E);
  static const Color primaryMid = Color(0xFF16213E);
  static const Color surfaceDark = Color(0xFF0F0F1A);
  static const Color cardDark = Color(0xFF1E1E32);

  // Accent — gold/amber
  static const Color accent = Color(0xFFD4A843);
  static const Color accentLight = Color(0xFFE8C96A);
  static const Color accentDark = Color(0xFFB8892E);

  // Text
  static const Color textPrimary = Color(0xFFF5F5F5);
  static const Color textSecondary = Color(0xFFB0B0C0);
  static const Color textMuted = Color(0xFF6E6E80);

  // Functional
  static const Color divider = Color(0xFF2A2A40);
  static const Color overlay = Color(0x99000000);
  static const Color shimmer = Color(0xFF2A2A3E);

  // Category specific
  static const Color fencingColor = Color(0xFF2E7D32);
  static const Color eventsColor = Color(0xFF1565C0);
  static const Color printingColor = Color(0xFFE65100);
}
