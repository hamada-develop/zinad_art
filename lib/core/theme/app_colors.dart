import 'package:flutter/material.dart';

class AppColors {
  AppColors._();

  // Primary palette — dark cyber aesthetic
  static const Color primaryDark = Color(0xFF090A0F);
  static const Color primaryMid = Color(0xFF111424);
  static const Color surfaceDark = Color(0xFF06070B);
  static const Color cardDark = Color(0xFF15192E);

  // Accent — Neon Red
  static const Color accent = Color(0xFFFF0033);
  static const Color accentLight = Color(0xFFFF4D70);
  static const Color accentDark = Color(0xFFCC0029);

  // Secondary Accent — Neon Cyan/Blue
  static const Color secondaryAccent = Color(0xFF00E5FF);

  // Text
  static const Color textPrimary = Color(0xFFFFFFFF);
  static const Color textSecondary = Color(0xFFB3E5FC);
  static const Color textMuted = Color(0xFF5A7291);

  // Functional
  static const Color divider = Color(0xFF1E2640);
  static const Color overlay = Color(0xAA000000);
  static const Color shimmer = Color(0xFF1E2640);

  // Category specific
  static const Color fencingColor = Color(0xFF2E7D32);
  static const Color eventsColor = Color(0xFF1565C0);
  static const Color printingColor = Color(0xFFE65100);
}
